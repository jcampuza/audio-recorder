{
  "name": "audio-recorder",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && workbox generateSW",
    "serve": "npm run build && vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^2.6.4",
    "workbox-cli": "^6.3.0"
  },
  "dependencies": {
    "localforage": "^1.10.0",
    "rxjs": "^7.4.0"
  }
}
